{
  "name": "sony-camera-bridge",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace=packages/bridge",
    "bridge": "npm run dev --workspace=packages/bridge",
    "web": "npm run dev --workspace=packages/web-rcp",
    "dev": "npm run bridge & npm run web",
    "companion-module": "npm run build --workspace=packages/companion-module-sony-camera-bridge",
    "companion-module:dev": "npm run dev --workspace=packages/companion-module-sony-camera-bridge",
    "electron": "npm run dev --workspace=packages/electron-app",
    "electron:build": "npm run build --workspace=packages/web-rcp && npm run dist --workspace=packages/electron-app",
    "electron:dist": "npm run build --workspace=packages/web-rcp && npm run dist:win --workspace=packages/electron-app"
  }
}
