{
  "name": "companion-module-lz-camera-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node scripts/sync-dev.mjs",
    "test": "tsx --test test/*.test.ts",
    "build:local": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@companion-module/base": "~1.14.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "license": "UNLICENSED"
}
